手書き数字画像認識コンテスト用データの利用方法

------------------------------------------------------------
import numpy as np

X = np.loadtxt("le4MNIST_X.txt")
print (X.shape) # (10000,784)
# もし，(10000,28,28)のデータが必要であればreshapeしてください．
X = X.reshape(10000,28,28)
print (X.shape) # (10000,28,28)